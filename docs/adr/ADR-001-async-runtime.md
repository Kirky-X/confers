# ADR-001: 异步运行时选择

## 状态
Accepted

## 背景
需要为异步操作选择运行时。Rust 生态中有多种异步运行时可选：tokio、async-std、glommio 等。

## 决策
使用 tokio 作为异步运行时。同步使用场景不依赖 tokio，允许用户在没有异步需求时避免 tokio 依赖。

## 后果
- 正面：tokio 成熟稳定，生态丰富，文档完善
- 正面：与大多数 Rust 异步库兼容
- 负面：二进制体积增加（可通过特性裁剪优化）
- 负面：运行时开销相对较高

## 相关
- ADR-002: Trait sync/async 分离
