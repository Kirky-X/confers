# 注意：以下是示例加密数据，格式为 enc:<算法>:<IV>:<密文>
# 实际使用时，请使用 confers encrypt 命令生成真实的加密数据
# 示例：confers encrypt config.toml --key-file secret.key
api_key = "enc:AES256GCM:abc123:xyz789"
database_password = "enc:AES256GCM:def456:uvw012"
